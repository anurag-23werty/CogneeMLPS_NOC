"""
Synthetic incident history for the demo.

The story: INC-1042 (new, "today") hits the same router as INC-1003 (historical,
"3 weeks ago") with the same underlying failure mode. The other 17 incidents
span 13 different devices, 6 sites, and a mix of BGP/STP/DHCP/VPN/QoS/hardware
failure modes deliberately -- so recall has to pick one relevant incident out
of a real haystack instead of just echoing a handful of examples back.

Run `python scripts/seed_memory.py` to load HISTORICAL_INCIDENTS into Cognee,
then feed NEW_INCIDENT through the agent graph to watch it get recalled.
"""

from datetime import datetime, timedelta,UTC
from app.schemas import Incident

_now = datetime.now(UTC)

HISTORICAL_INCIDENTS = [
    Incident(
        incident_id="INC-1003",
        timestamp=_now - timedelta(days=21),
        device="MPLS-RTR-07",
        site="DEL-CORE",
        symptom="BGP session to upstream peer flapping every 4-6 minutes after firmware upgrade to 17.9.2",
        telemetry_snippet="BGP-5-ADJCHANGE logged 14 times in 90 min, interface Gi0/0/1 MTU reported as 1500 vs peer 9000",
        severity="major",
        root_cause="Jumbo MTU mismatch introduced by firmware upgrade reset the sub-interface MTU to default 1500, causing BGP keepalives to fragment and drop under load",
        resolution="Manually reapplied MTU 9216 on Gi0/0/1 and added a post-upgrade config-check step to the runbook",
    ),
    Incident(
        incident_id="INC-1011",
        timestamp=_now - timedelta(days=17),
        device="SDWAN-EDGE-14",
        site="BLR-BR2",
        symptom="Intermittent brownout on primary WAN link, failover to backup not triggering",
        telemetry_snippet="Latency spikes to 900ms for 30s windows, packet loss 12%, BFD timer not tripping",
        severity="major",
        root_cause="Fiber micro-bend near the ISP handoff causing partial signal degradation below BFD's loss threshold",
        resolution="ISP replaced the patch fiber, tightened BFD multiplier from 5 to 3",
    ),
    Incident(
        incident_id="INC-1019",
        timestamp=_now - timedelta(days=12),
        device="CORE-SW-02",
        site="DEL-CORE",
        symptom="Voice VLAN packet loss during business hours",
        telemetry_snippet="QoS queue drops on Gi1/0/3, EF class exceeding policed rate during 10am-1pm peak",
        severity="warning",
        root_cause="QoS policy-map wasn't updated after a new video conferencing rollout increased EF-marked traffic volume",
        resolution="Re-policed EF queue from 10Mbps to 25Mbps, added marking ACL for the new app",
    ),
    Incident(
        incident_id="INC-1026",
        timestamp=_now - timedelta(days=8),
        device="MPLS-RTR-19",
        site="HYD-BR1",
        symptom="Interface flapping on trunk to access switch",
        telemetry_snippet="Duplex mismatch detected, Gi0/1 half-duplex vs peer full-duplex, CRC errors climbing",
        severity="warning",
        root_cause="Access switch NIC auto-negotiation failure after a hardware swap",
        resolution="Forced full-duplex/1000 on both ends, replaced the NIC",
    ),
    Incident(
        incident_id="INC-1031",
        timestamp=_now - timedelta(days=5),
        device="PSU-MON-03",
        site="BLR-BR2",
        symptom="Redundant PSU failure alarm on core switch",
        telemetry_snippet="PSU2 output voltage dropped to 0V, PSU1 carrying full load, chassis temp nominal",
        severity="critical",
        root_cause="PSU2 hardware failure, no environmental cause found",
        resolution="RMA'd PSU2, confirmed load-sharing restored",
    ),
    Incident(
        incident_id="INC-1038",
        timestamp=_now - timedelta(days=2),
        device="SDWAN-EDGE-22",
        site="HYD-BR1",
        symptom="Application SLA breach for ERP traffic",
        telemetry_snippet="SD-WAN path selection kept preferring MPLS path with 180ms latency over cheaper broadband at 40ms",
        severity="warning",
        root_cause="Stale SLA policy still weighted for legacy MPLS-first routing from before broadband circuit was upgraded",
        resolution="Updated SD-WAN policy to prefer broadband path for ERP class",
    ),
    Incident(
        incident_id="INC-0987",
        timestamp=_now - timedelta(days=29),
        device="CORE-SW-05",
        site="PUNE-BR3",
        symptom="Spanning tree topology change flood",
        telemetry_snippet="STP TCN packets spiking to 40/min, three access switches oscillating between blocking and forwarding",
        severity="warning",
        root_cause="A newly added switch had a lower bridge priority than intended, contesting root bridge election",
        resolution="Set explicit root bridge priority on core switch, documented STP priority scheme",
    ),
    Incident(
        incident_id="INC-0991",
        timestamp=_now - timedelta(days=27),
        device="FW-EDGE-03",
        site="MUM-DC1",
        symptom="Elevated firewall CPU during backup window",
        telemetry_snippet="CPU pegged at 95% from 1-3am, deep packet inspection queue depth maxed",
        severity="warning",
        root_cause="Backup traffic wasn't excluded from DPI policy, forcing full inspection on high-throughput bulk transfer",
        resolution="Added backup subnet to DPI bypass list",
    ),
    Incident(
        incident_id="INC-0999",
        timestamp=_now - timedelta(days=25),
        device="MPLS-RTR-11",
        site="CHN-BR4",
        symptom="One-way audio on VoIP calls to a specific branch",
        telemetry_snippet="RTP stream asymmetric, inbound packets present, outbound dropped at NAT boundary",
        severity="warning",
        root_cause="SIP ALG on the edge router was rewriting SDP incorrectly after a config sync",
        resolution="Disabled SIP ALG, added static NAT rule for the SBC",
    ),
    Incident(
        incident_id="INC-1004",
        timestamp=_now - timedelta(days=23),
        device="SDWAN-EDGE-08",
        site="PUNE-BR3",
        symptom="Tunnel flapping between two SD-WAN edges",
        telemetry_snippet="IPsec re-key failures every 8 minutes, phase 2 SA mismatch logged",
        severity="major",
        root_cause="Clock drift between the two edges exceeded the IKE replay window after NTP source was unreachable",
        resolution="Restored NTP reachability, added secondary NTP source",
    ),
    Incident(
        incident_id="INC-1007",
        timestamp=_now - timedelta(days=20),
        device="CORE-SW-09",
        site="MUM-DC1",
        symptom="Multicast video stream drops for trading floor displays",
        telemetry_snippet="IGMP snooping table entries expiring early, querier election flapping between two switches",
        severity="warning",
        root_cause="Two switches both configured as IGMP querier with the same priority, causing periodic re-election",
        resolution="Disabled querier on the secondary switch, kept single authoritative querier",
    ),
    Incident(
        incident_id="INC-1014",
        timestamp=_now - timedelta(days=19),
        device="FW-EDGE-07",
        site="HYD-BR1",
        symptom="VPN client connections rejected intermittently",
        telemetry_snippet="RADIUS auth timeouts, 3 of 40 auth attempts failing per hour",
        severity="warning",
        root_cause="RADIUS server pool had one unhealthy node still in rotation without health checks",
        resolution="Enabled RADIUS health checks, removed unhealthy node from pool",
    ),
    Incident(
        incident_id="INC-1016",
        timestamp=_now - timedelta(days=18),
        device="MPLS-RTR-03",
        site="MUM-DC1",
        symptom="Asymmetric routing causing stateful firewall drops",
        telemetry_snippet="Return traffic taking a different MPLS path than outbound, firewall session table missing state",
        severity="major",
        root_cause="A route-map change during a maintenance window altered outbound path preference without updating the paired inbound policy",
        resolution="Aligned inbound/outbound route-maps, added change-review step for paired policies",
    ),
    Incident(
        incident_id="INC-1021",
        timestamp=_now - timedelta(days=15),
        device="CORE-SW-02",
        site="DEL-CORE",
        symptom="Slow DHCP lease assignment for new devices",
        telemetry_snippet="DHCP discover-to-offer latency averaging 4.5s, relay agent queue backing up",
        severity="warning",
        root_cause="DHCP scope was near exhaustion, server spending cycles scanning for reclaimable leases",
        resolution="Expanded scope size, shortened stale lease reclaim interval",
    ),
    Incident(
        incident_id="INC-1023",
        timestamp=_now - timedelta(days=14),
        device="SDWAN-EDGE-14",
        site="BLR-BR2",
        symptom="Cloud SaaS app slowness reported by users",
        telemetry_snippet="DNS resolution for SaaS domain taking 900ms via on-prem resolver vs 40ms via public resolver",
        severity="warning",
        root_cause="On-prem DNS forwarder had a stale conditional forwarder pointing to a decommissioned resolver",
        resolution="Removed stale forwarder entry, pointed to current upstream resolvers",
    ),
    Incident(
        incident_id="INC-1029",
        timestamp=_now - timedelta(days=7),
        device="FW-EDGE-03",
        site="MUM-DC1",
        symptom="Site-to-site VPN down after ISP maintenance",
        telemetry_snippet="IKE phase 1 negotiation failing, peer public IP changed but local config still pointed to old IP",
        severity="major",
        root_cause="ISP performed an unannounced IP reassignment on the remote peer's circuit",
        resolution="Updated peer IP in VPN config, requested static IP assignment from ISP",
    ),
    Incident(
        incident_id="INC-1033",
        timestamp=_now - timedelta(days=4),
        device="CORE-SW-05",
        site="PUNE-BR3",
        symptom="Guest wifi VLAN unreachable from access points",
        telemetry_snippet="Trunk port pruning guest VLAN after switch stack member rejoined with old config",
        severity="warning",
        root_cause="Stack member rejoined with a cached VLAN database missing the guest VLAN added last week",
        resolution="Forced VLAN database sync across stack, enabled auto-sync on rejoin",
    ),
    Incident(
        incident_id="INC-1036",
        timestamp=_now - timedelta(days=3),
        device="MPLS-RTR-11",
        site="CHN-BR4",
        symptom="Routing protocol adjacency down after power event",
        telemetry_snippet="OSPF neighbor state stuck in EXSTART, MTU mismatch on newly replaced line card",
        severity="major",
        root_cause="Replacement line card shipped with a different default MTU than the rest of the chassis",
        resolution="Standardized MTU across all line cards, added to hardware replacement checklist",
    ),
]

# The incident we feed live through the agent graph during the demo.
# Same device + same failure signature as INC-1003, three weeks later.
NEW_INCIDENT = Incident(
    incident_id="INC-1042",
    timestamp=_now,
    device="MPLS-RTR-07",
    site="DEL-CORE",
    symptom="BGP session to upstream peer flapping every few minutes, started right after a scheduled firmware patch",
    telemetry_snippet="BGP-5-ADJCHANGE logged 9 times in 40 min, interface Gi0/0/1 MTU reads 1500",
    severity="major",
)
